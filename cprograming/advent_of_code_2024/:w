#include <stdio.h>
#include <stdlib.h>

int main() {
	FILE * fp = fopen("./input2.txt", "r");
	
	char * buff;
	unsigned long size;

	while(getline(&buff, &size, fp) != -1) {
		if()
	}

	fclose(fp);
}
